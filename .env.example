# Required API keys
GROQ_API_KEY=
TAVILY_API_KEY=
EXA_API_KEY=

# Optional — override default models (defaults shown)
MAIN_MODEL_NAME=llama-3.3-70b-versatile
REVIEW_MODEL_NAME=llama-3.1-8b-instant

# Optional — server config (defaults shown)
PORT=8000
CORS_ORIGINS=http://localhost:8501,http://localhost:8502

# On Render/Streamlit Cloud: set these in the platform dashboard, not in a .env file
